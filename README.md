# Когда первый раз и development режим
* ```yarn```
* ```yarn start```

# Установка демона и startup:
* ```npm i pm2 -g```
* ```pm2 startup```

# Команды для включения и остановки демона
* ```yarn application-start```
* ```yarn application-stop```
